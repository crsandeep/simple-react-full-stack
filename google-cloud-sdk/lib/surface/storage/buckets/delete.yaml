- release_tracks: [ALPHA]

  help_text:
    brief: Delete a Cloud Storage bucket.
    description: Delete a Cloud Storage bucket.

  request:
    collection: storage.buckets

  arguments:
    resource:
      help_text: The bucket you want to delete.
      spec: !REF googlecloudsdk.command_lib.storage.resources:bucket
