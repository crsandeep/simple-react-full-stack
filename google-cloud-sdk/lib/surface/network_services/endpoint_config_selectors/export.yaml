release_tracks: [ALPHA]
help_text:
  brief: |
    Export endpoint config selector.
  description: |
    Export an endpoit config selector.

request:
  collection: networkservices.projects.locations.endpointConfigSelectors
  api_version: v1alpha1

arguments:
  resource:
    spec: !REF googlecloudsdk.command_lib.network_services.resources:endpointConfigSelector
    help_text: |
      Name of the endpoint config selector to export.
